apiVersion: v1
kind: Service
metadata:
  name: genai-service
  labels:
    app.kubernetes.io/name: genai-service
    app.kubernetes.io/part-of: petclinic
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: genai-service
    app.kubernetes.io/part-of: petclinic
  ports:
    - name: http
      port: 8084
      targetPort: http