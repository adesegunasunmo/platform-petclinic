apiVersion: v1
kind: Service
metadata:
  name: discovery-server
  labels:
    app.kubernetes.io/name: discovery-server
    app.kubernetes.io/part-of: petclinic
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: discovery-server
    app.kubernetes.io/part-of: petclinic
  ports:
    - name: http
      port: 8761
      targetPort: http